# run script when files changed
- name: bar
  match:
    - "_foo"
  run:
    - cp -f _foo _bar; echo "Hello World"

# run script or delegate to watch script
- name: baz
  run:
    - cp -f _foo _baz
  delegate:
    - watchman-make -p "_foo" --run "cp -f _foo _baz"
